.main-box-content.clearfix
  - @items && @items.each do |item|
    %section.item-content
      = link_to item_path(item.id), {class: 'item-content__photo'} do
        %figure.item-content__photo
          -# = image_tag item.image.url
        .item-content-body
          %h3.item-content-body__name #{item.name}
          .item-content-body__num
            .item-content-body__num__price ¥ #{item.price}
            .item-content-body__num__icon
              %i.far.fa-heart
              %span 2
          %p.item-content-body__num__tax （税込）

.main-all
  = link_to'すべての商品をみる', 'https://www.mercari.com/jp/category/1/'
